@model ColorScheme.Models.ColorSchemeM

@{
    ViewData["Title"] = "Index";
    Layout = "_Layout";
}

<div class="row">
    <div class="col-md-4">
        @*<form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">*@
                <label asp-for="SchemeType" class="control-label"></label>
                <select asp-for="SchemeType" class="form-control" asp-items="@Html.GetEnumSelectList<ColorScheme.Models.SchemeType>()"></select>
                <span asp-validation-for="SchemeType" class="text-danger"></span>
            @*</div>
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-primary" />
            </div>
        </form>*@
    </div>
</div>


@*<p>
        <a asp-action="Create">Create New</a>
    </p>*@
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SchemeType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ColorSchemes)
            </th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.SchemeType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ColorSchemes)
                </td>
                <td>*@
@*<a asp-action="Edit" asp-route-id="@item.ID">Save</a> |*@

@*</td>
                </tr>
            }
        </tbody>
    </table>*@